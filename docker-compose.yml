services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: packinglist
      POSTGRES_PASSWORD: packinglist
      POSTGRES_DB: packinglist
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
